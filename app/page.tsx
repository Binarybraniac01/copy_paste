"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function Home() { const router = useRouter(); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); async function create() { setBusy(true); setError(""); try { const response = await fetch("/api/pads", { method: "POST" }); const data = await response.json(); if (!response.ok) throw new Error(data.error); router.push(`/p/${data.sessionId}`); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to create a pad."); setBusy(false); } }
return <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center p-6"><h1 className="text-3xl font-semibold tracking-tight">BridgePad</h1><p className="mt-3 max-w-xl text-slate-600">A temporary plain-text shared note. Open the same session from a Normal URL or Isolation URL to keep both views in sync.</p><button onClick={create} disabled={busy} className="mt-8 w-fit rounded-md bg-slate-900 px-4 py-2.5 font-medium text-white disabled:opacity-60">{busy ? "Creating…" : "Create New Pad"}</button>{error && <p role="alert" className="mt-4 text-red-700">{error}</p>}</main>; }
